# Slot-frame-Tree-Sim
A Python simulation of an IoT tree network where parent nodes schedule child transmissions using round-robin slot allocation. The project models packet generation, buffer behavior, and end-to-root delivery over multiple slotframe lengths to compare performance metrics like delivery ratio and buffer occupancy.
